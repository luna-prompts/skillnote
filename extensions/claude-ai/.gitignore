# Build output — regenerated by `npm run build`.
dist/

# Dependencies — pinned by package-lock.json, installed by `npm install`.
node_modules/

# Logs + caches.
*.log
.DS_Store
.vite/
coverage/

# Captured-endpoint snapshots are written by the developer-only script in
# scripts/capture-endpoints.mjs to help us re-verify the claude.ai REST
# contract. Always per-machine, never shippable.
scripts/captured-endpoints.md
